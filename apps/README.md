# App Scaffolds

This folder contains two SwiftUI starter apps:

- ios
- macos

Each app is a SwiftPM executable that depends on the local Coinswap package.
Open the folder in Xcode 15+ and run it on a simulator or device.
